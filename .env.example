# Optional: connect Supabase for multi-device sync + Teacher Dashboard (Phase 2).
# Without these, the app runs fully offline using the browser's localStorage.
VITE_SUPABASE_URL=
VITE_SUPABASE_ANON_KEY=
